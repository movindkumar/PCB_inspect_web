name: flutter_application_1
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  firebase_storage: ^12.4.10

  cupertino_icons: ^1.0.8
  
  # API and file handling
  http: ^1.1.0
  image_picker: ^1.0.0
  file_picker: ^8.0.7
  
  # Firebase
  firebase_core: ^3.1.0
  firebase_database: ^11.0.0
  cloud_firestore: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/pcb_viewer.html